{
  "name": "web-design-test-1.0",
  "version": "1.0.0",
  "private": true,
  "description": "Parallax esports mouse landing page with local preview and screenshot tooling.",
  "scripts": {
    "preview": "python3 -m http.server 4173 --bind 127.0.0.1",
    "capture": "node scripts/capture.mjs"
  },
  "devDependencies": {
    "playwright": "^1.54.2"
  }
}
